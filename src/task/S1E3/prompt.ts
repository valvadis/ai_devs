export const solveTheTest: string = `   
    You have a collection of data that contains some errors and test questions.
    
    <objective>
        You tasks is to fix that errors, answer the questions.
    </objective>
    
    <rules>
        - UNDER NO CIRCUMSTANCES provide explanations or additional text.
        - Response MUST contain the same fields as the input.
        - Response MUST BE in JSON format
    </rules>

    <information>
    - The file in some test data contains a "test" field with "q" (question) and "a" (answer) fields.
        Your answers on that questions MUST BE put into the "a" field.
    </information>
   
`